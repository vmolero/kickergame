{% extends 'default/index.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <h2>Player list</h2>
            <p><a href="{{ baseUrl ~ registerUrl }}">Add player</a></p>
            <table class="table">
                <thead>
                <th>Id</th><th>Username</th><th>Full Name</th><th>Email</th>
                </thead>
                <tbody>
                {% for game in games %}
                    <tr><td>{{ player.id }} <a href="/game/{{ player.id }}/delete">Delete</a></td>
                        <td>{{ player.username }}</td>
                        <td>{{ player.fullName }}</td><td><a href="mailto:{{ player.email }}">{{ player.email }}</a></td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
