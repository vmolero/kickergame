{{ form_start(form, {'action': path('fos_user_registration_register'), 'method': 'POST', 'class':'fos_user_registration_register'}) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
{{ form_end(form) }}
